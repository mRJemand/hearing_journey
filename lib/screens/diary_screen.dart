import 'package:flutter/material.dart';

class DiaryScreen extends StatelessWidget {
  static const routeName = '/diary';
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Tagebuch!'));
  }
}
